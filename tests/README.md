# Tests to implement
general determinism
 - make a list of random life-like rules
 - run via tick + hash
 - include phase diagram with smoothstep, deterministic bump function
 - read a grid state for initialization + an object with { step: 4, hash : 'asdasdas' }

terminal-life tests
 - create images of differeing sizes - the ones for the readme.
 - verify the file hashes are identical

command bus test
 - run a bunch of command bus commands and verify end hashes. i.e. phase diagram moving, etc.
 - as full coverage of commands as possible
