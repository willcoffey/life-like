{
  "scripts": {
    "host": "vite --host --port 3000",
    "dev": "nodemon --watch ./src --ext '*' --exec 'npm run host'"
  },
  "dependencies": {
    "vimlike-keybinder": "github:willcoffey/vimlike-keybinder"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
